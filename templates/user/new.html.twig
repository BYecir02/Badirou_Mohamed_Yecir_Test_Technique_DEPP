{% extends 'base.html.twig' %}

{% block title %}Ajouter un utilisateur{% endblock %}

{% block body %}
    <div class="form-page">
        <div class="page-header">
            {{ include('_ariane.html.twig', { items: [
                { label: 'Utilisateurs',        url: path('app_user_index') },
                { label: 'Ajouter un utilisateur', url: null }
            ]}) }}

            <h1>Ajouter un utilisateur</h1>
            <p>Créez un nouveau compte utilisateur.</p>
        </div>

        {{ include('user/_form.html.twig', {
            button_label: 'Créer l’utilisateur',
            password_required: true,
            cancel_path: path('app_user_index')
        }) }}
    </div>
{% endblock %}